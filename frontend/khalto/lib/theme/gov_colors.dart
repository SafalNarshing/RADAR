import 'package:flutter/material.dart';

/// Shared design tokens for the Government/Police screens, matching the
/// tokens already used across the citizen-facing RADAR screens.
class GovColors {
  static const background = Color(0xFFF7F9FB);
  static const primary = Color(0xFF0F2B46);
  static const accent = Color(0xFFE53935);
  static const card = Colors.white;
  static const border = Color(0xFFEAEAEA);
  static const textPrimary = Color(0xFF111111);
  static const textSecondary = Color(0xFF6B7280);
  static const chipBg = Color(0xFFF6F7FB);
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
}
